# CryptoBookKeeper - Environment Configuration Template
# Copy this file to .env and fill in your actual values

# =============================================================================
# DATA EXPORT CONFIGURATION
# =============================================================================

# Start timestamp for data export (ISO-8601 UTC format)
# All transactions from this date onwards will be exported
START_TS=2024-01-01T00:00:00Z

# =============================================================================
# EXCHANGE API CONFIGURATION
# =============================================================================

# Comma-separated list of exchanges to export data from
# Supported: binance, kraken, coinbase, bitfinex, huobi, okx, bybit
EXCHANGES=coinbase

# Exchange API Keys (read-only recommended)
# Only configure the exchanges you're using

# Binance API Keys
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_API_SECRET=your_binance_api_secret_here

# Kraken API Keys
KRAKEN_API_KEY=your_kraken_api_key_here
KRAKEN_API_SECRET=your_kraken_api_secret_here

# Coinbase API Keys
COINBASE_API_KEY=your_coinbase_api_key_here
COINBASE_API_SECRET=your_coinbase_api_secret_here

# Bitfinex API Keys
BITFINEX_API_KEY=your_bitfinex_api_key_here
BITFINEX_API_SECRET=your_bitfinex_api_secret_here

# Huobi API Keys
HUOBI_API_KEY=your_huobi_api_key_here
HUOBI_API_SECRET=your_huobi_api_secret_here

# OKX API Keys
OKX_API_KEY=your_okx_api_key_here
OKX_API_SECRET=your_okx_api_secret_here

# Bybit API Keys
BYBIT_API_KEY=your_bybit_api_key_here
BYBIT_API_SECRET=your_bybit_api_secret_here

# =============================================================================
# BLOCKCHAIN CONFIGURATION - DeBank Cloud API
# =============================================================================

# DeBank API Key (for comprehensive on-chain transaction data)
# Sign up at: https://cloud.debank.com/
# Purchase units with USDC, then copy your Access Key
DEBANK_API_KEY=your_debank_access_key_here

# Comma-separated list of blockchain chains to export
# Leave this field to export from your preferred chains only
# 
# POPULAR CHAINS (recommended for most users):
# eth,bsc,matic,arb,op,base,avax,ftm,cro
#
# ALL 123 SUPPORTED CHAINS:
# eth, bsc, matic, arb, op, base, avax, ftm, cro, xdai, manta, blast, linea,
# scrl, era, pze, bob, zora, mode, taiko, nova, lisk, boba, metis, kava,
# mnt, sonic, frax, cyber, xlayer, ink, opbnb, mint, plume, zircuit, morph,
# core, merlin, b2, btr, bb, swell, itze, sei, tlos, orderly, ape, world,
# hsk, lens, uni, corn, duck, sophon, abs, dbk, katana, soneium, vana, hyper,
# hemi, goat, gravity, story, plasma, mito, karak, reya, zero, rari, tac,
# dfk, chiliz, canto, bera, astar, klay, shib, ron, celo, wemix, movr, mobm,
# oas, fuse, cfx, iotx, flr, croze, rsk, doge, xrpl, ethlink, zeta
#
# For details on each chain, visit: https://docs.cloud.debank.com/
DEBANK_CHAINS=eth,bsc,matic,arb,op,base,avax

# Filter out scam/spam tokens (recommended: true)
# DeBank marks suspicious tokens with is_scam flag
DEBANK_FILTER_SCAMS=true

# Comma-separated list of EVM addresses to monitor
# Only transactions involving these addresses will be exported
EVM_ADDRESSES=0xYourEthereumAddress1,0xYourEthereumAddress2

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# DuckDB database path
DUCKDB_PATH=./data/cryptobookkeeper.duckdb

# =============================================================================
# EXPORT CONFIGURATION
# =============================================================================

# Rate limiting (requests per minute per exchange)
# Adjust based on your API limits
RATE_LIMIT_PER_MINUTE=60

# Batch size for API requests
BATCH_SIZE=100

# Maximum retries for failed API requests
MAX_RETRIES=3

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Log file paths
LOG_FILE=./logs/cryptobookkeeper.log
